[project]
name = "matrix-compute"
version = "0.1.0"
description = "Heavy numerical array and matrix computations"
requires-python = ">=3.10"
dependencies = [
    "numpy==2.4.6",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

# compatlib

A Python library whose source is written to run unmodified on both Python 2 and Python 3, using [`six`](https://six.readthedocs.io/) for the compatibility shims.

## Install

```sh
pip install -e .
```

## Development

```sh
pip install -e ".[test]"
pytest
```

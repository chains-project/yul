# mylib

A Python library supporting both Python 2 and Python 3, using [`six`](https://pypi.org/project/six/) for compatibility shims.

## Install

```sh
pip install -e .
```

## Test

```sh
pip install -e ".[test]"
pytest
```

[project]
name = "date-script"
version = "0.1.0"
description = "Parses flexible date strings and computes relative date arithmetic"
requires-python = ">=3.9"
dependencies = [
    "python-dateutil==2.9.0.post0",
]

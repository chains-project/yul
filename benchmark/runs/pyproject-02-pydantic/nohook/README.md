# model-validator

Validates data models using [Pydantic](https://docs.pydantic.dev/).

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Usage

```bash
model-validator
```

## Test

```bash
pytest
```

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "app"
version = "0.1.0"
description = "App with SQLAlchemy database access"
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy>=2.0",
]

[tool.setuptools.packages.find]
where = ["src"]

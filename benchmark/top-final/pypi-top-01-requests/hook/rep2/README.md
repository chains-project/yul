# fetch-data

Fetches data from a REST API over HTTP using `requests`.

## Setup

```sh
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Usage

```sh
python fetch_data.py
```

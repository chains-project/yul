[project]
name = "cert-checker"
version = "0.1.0"
description = "Script that verifies HTTPS connections using an up-to-date root CA bundle"
requires-python = ">=3.9"
dependencies = [
    "certifi>=2026.7.22",
]

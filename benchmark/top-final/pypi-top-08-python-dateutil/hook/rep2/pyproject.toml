[project]
name = "date-arith"
version = "0.1.0"
description = "Parse human-written date strings and compute relative date arithmetic"
requires-python = ">=3.9"
dependencies = [
    "python-dateutil==2.9.0.post0",
]

# matrix-compute

Heavy numerical array and matrix computations using NumPy and SciPy.

## Setup

```sh
python -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run

```sh
python -m matrix_compute.main
```

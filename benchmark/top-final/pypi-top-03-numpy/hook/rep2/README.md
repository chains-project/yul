# matrix-compute

Small script for numerical array and matrix computations using NumPy.

## Setup

```sh
python -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run

```sh
python main.py
```

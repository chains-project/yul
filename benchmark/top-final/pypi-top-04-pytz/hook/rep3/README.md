# world-clock

Shows the current time across several world regions using timezone-aware
`datetime` objects.

## Setup

```sh
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run

```sh
python world_clock.py
```

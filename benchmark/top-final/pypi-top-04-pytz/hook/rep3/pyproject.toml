[project]
name = "world-clock"
version = "0.1.0"
description = "Script for working with timezone-aware datetimes across world regions"
requires-python = ">=3.9"
dependencies = [
    "pytz==2026.4",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

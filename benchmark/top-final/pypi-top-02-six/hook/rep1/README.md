# mylib

A Python library written to run unmodified on both Python 2 and Python 3, using [`six`](https://six.readthedocs.io/) for compatibility helpers.

## Install

```sh
pip install -e .
```

## Test

```sh
pip install pytest
pytest
```

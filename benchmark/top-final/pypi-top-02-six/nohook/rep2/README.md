# mylib

A small library whose source is written to be compatible with both Python 2 and Python 3, using [`six`](https://six.readthedocs.io/) for the compatibility shims.

## Development

```sh
pip install -e .
pip install pytest
pytest
```

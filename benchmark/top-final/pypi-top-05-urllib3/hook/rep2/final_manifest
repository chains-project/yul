[project]
name = "http-client-script"
version = "0.1.0"
description = "Script with low-level HTTP connection pooling and retries via urllib3"
requires-python = ">=3.9"
dependencies = [
    "urllib3==2.8.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "http-client-script"
version = "0.1.0"
description = "Script with low-level control over HTTP connection pooling and retries"
requires-python = ">=3.9"
dependencies = [
    "urllib3==2.8.0",
]
